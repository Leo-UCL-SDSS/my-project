readme_text = """\
# Henan COVID-19 Map

## Overview
This project uses `pyecharts` to visualize COVID-19 case distribution across Henan province.

## Files
- `henan_covid.txt`: Raw JSON data file containing case numbers by city.
- `map_script.py`: Python script that reads data and generates the map.
- `henan_covid_map.html`: Output interactive HTML map.

## Requirements
- Python 3.x
- `pyecharts` (install via `pip install pyecharts`)

## Usage
1. Place `henan_covid.txt` in the same directory as `map_script.py`.
2. Run the script:
   ```bash 
   python map_script.py"""
